import 'package:flutter/material.dart';

const kAppColor = Colors.blue;
const kMyPadding = EdgeInsets.symmetric(horizontal: 30.0, vertical: 20.0);
const kRadius = Radius.circular(30);
const kFontSize = 20.0;
